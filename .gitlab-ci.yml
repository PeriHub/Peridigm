job:
  tags:
    - wsl
  before_script:
    - apt update && apt install -y g++ git cmake 
  script:
    - cd /usr/local/src/peridigmVsBuild/src/materials/unit_test
    - ctest -T Test
    - cd Testing
    - $TIMESTAMP=$(head -n 1 TAG)
    - echo $TIMESTAMP
    - cd $TIMESTAMP

